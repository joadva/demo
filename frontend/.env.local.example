# URL del API desplegado, sin barra final. Es el output ApiURL del stack:
# lo trae aws-exports.json (npm run env:aws) o `aws cloudformation describe-stacks`.
NEXT_PUBLIC_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/api
